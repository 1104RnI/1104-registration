import styled from 'styled-components'

export const EmailCheckContainer = styled.form`
	width: 100%;
	max-width: 37.5rem;
	display: flex;
	flex-direction: column;
	gap: 4rem;

	label {
		display: flex;
		flex-direction: column;

		gap: 1.5rem;
	}
`
